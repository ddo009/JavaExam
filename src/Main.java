public class Main {



        // 어레이리스트를 추가함
        // <> 사이에는 타입을 설정
    //  ArrayList<String> list = new ArrayList<>();

        // list에 a,b,c를 순서대로 추가함
        // 마지막에 추가한 add 가 tail 이 됌
    //  list.add("a");
    //  list.add("b");
    //  list.add("c");
        // 원하는 곳에다가 원하는 입력값을 추가
    //  list.add(2, "d");

    //  String str = "name";
        // string 변수를 list 에 추가하거나 삭제가능함.
    // list.add(str);
    // list.remove(str);
        // 또는 넘버로도 삭제처리가능
    //  System.out.println(list.remove(2) + "가 삭제");
    //  System.out.println(list);

        // list.get 으로 해당 번호에 들어간 문자열 찾기가능
    //  System.out.println(list.get(0) + "찾았음");


}
